# `firmware/include/`

Project-wide headers. Currently empty — application code lives in `src/main.cpp` and pulls libraries from PlatformIO's registry (`platformio.ini`).

When the codebase grows past one translation unit, shared declarations land here.
