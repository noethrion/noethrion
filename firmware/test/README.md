# `firmware/test/`

Unity-based unit tests, run via `pio test` (native env) or `pio test -e esp32dev` (on hardware).

Currently empty — the next firmware milestone (real meter integration + signing) is what makes test scaffolding meaningful. For the probe-only skeleton, manual serial monitor inspection covers the surface.
